<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
        <title>PetLandia</title>
        
        <!-- Links -->
        <script src="https://kit.fontawesome.com/b29db1d018.js" crossorigin="anonymous"></script>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/register.css">
        <link rel="stylesheet" href="/css/normalize.css"> 
        <link rel="stylesheet" href="/css/styles.css">
        <link rel="stylesheet" href="/css/responsive.css">
    </head>

    <body>
        <%- include('./partials/header.ejs') %> <!--include header-->

        <main class="main-chart">
            <div class="up-chart">
                <h2>registrarse <i class="fa-solid fa-dog"></i></h2>
            </div>

            <div class="register-container">
                <img src="" alt="">

                <form method="post" action="/usuario/register" enctype="multipart/form-data" class="register-form">
                    <div class="user-info">
                        <label for="nombre">Nombre:</label>
                        <input type="text" id="name" name="nombre" value="<%= locals.ultData ? ultData.nombre : null %>">
                        <div class="color-alert">
                            <% if(locals.errores && errores.nombre){ %>
                                <%= errores.nombre.msg %>
                            <% } %>
                        </div>
                    
                        <label for="apellido">Apellido:</label>
                        <input type="text" id="surname" name="apellido" value="<%= locals.ultData ? ultData.apellido : null %>">
                        <div class="color-alert">
                            <% if(locals.errores && errores.apellido){ %>
                                <%= errores.apellido.msg %>
                            <% } %>
                        </div>

                        <label for="email">Correo Electrónico:</label>
                        <input type="text" id="email" name="email" value="<%= locals.ultData ? ultData.email : null %>">
                        <div class="color-alert">
                            <% if(locals.errores && errores.email){ %>
                                <%= errores.email.msg %>
                            <% } %>
                        </div>

                        <label for="telefono">Telefono:</label>
                        <input type="text" id="telefono" name="telefono" value="<%= locals.ultData ? ultData.telefono : null %>">
                        <div class="color-alert">
                            <% if(locals.errores && errores.telefono){ %>
                                <%= errores.telefono.msg %>
                            <% } %>
                        </div>

                        <label for="contrasena">Contraseña:</label>
                        <input type="password" id="password" name="contrasena">
                        <div class="color-alert">
                            <% if(locals.errores && errores.contrasena){ %>
                                <%= errores.contrasena.msg %>
                            <% } %>
                        </div>
                    
                        <label for="confirmar_contrasena">Confirmar Contraseña:</label>
                        <input type="password" id="confirmar_password" name="confirmarContrasena">
                        <div class="color-alert">
                        <% if(locals.errores && errores.confirmarContrasena){ %>
                            <%= errores.confirmarContrasena.msg %>
                        <% } %>
                        </div>

                        <label for="foto_perfil">Foto de Perfil:</label>
                        <input type="file" id="foto_perfil" name="fotoPerfil" class="photo">
                        <div class="color-alert">
                            <% if(locals.errores && errores.fotoPerfil){ %>
                                <%= errores.fotoPerfil.msg %>
                            <% } %>
                        </div> 
                    </div>
                    
                    <div class="bottom-info">
                        <button type="submit"> Registrarse <i class="fa-solid fa-paw"></i> </button>
                        <a href="/usuario/login"> O Iniciar sesion</a>
                    </div>
                </form>

            </div>
        </main>

        <%- include('./partials/footer.ejs') %> <!--include footer-->

    </body>
</html>